# Shirt textures

Drop shirt texture images here (PNG/JPG). Not wired up to anything yet —
see the "SHIRT TEXTURES" comment near `body-anchor`'s chest handling in
`games/pistols-at-dawn/index.html` for the intended hookup point once
there's an image to point it at.
